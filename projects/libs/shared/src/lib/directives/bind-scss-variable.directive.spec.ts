import { BindScssVariableDirective } from './bind-scss-variable.directive';

describe('BindScssVariableDirective', () => {
  it('should create an instance', () => {
    const directive = new BindScssVariableDirective();
    expect(directive).toBeTruthy();
  });
});
